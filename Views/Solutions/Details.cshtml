@using webjooneli.Models.Entities
@model BusinessModel

<div class="container my-5">
    <h2 class="fw-bold text-danger mb-4">@Model.Title</h2>

    <div class="bg-light p-4 rounded shadow-sm">
        <div class="mb-4">
            <i class="bi @Model.Icon text-danger fs-1"></i>
        </div>

        <p class="fs-5">
            @Model.Description
        </p>

        <a asp-controller="Home" asp-action="Index" class="btn btn-outline-danger mt-3">← Back to Business Verticals</a>
    </div>
</div>
