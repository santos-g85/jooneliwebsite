@model List<webjooneli.Models.Entities.NewsModel>

<div class="container">
    <h2 class="text-center fw-bold mb-3 display-4" style="background: linear-gradient(to right, #f44336, #4caf50); -webkit-background-clip: text; color: transparent;">
        News & Updates
    </h2>

    <div class="row">
        <!-- Featured Section -->
        <div class="col-md-7">
            <h4 class="fw-bold mb-3">Featured</h4>
            <div class="card border-0 shadow-sm">
                @if (Model != null && Model.Any())
                {
                    var featured = Model.OrderByDescending(n => n.CreatedAt).First();
                    <img src="/@featured.ImageId" class="card-img-top rounded" alt="Featured News" style="height:300px;">
                    <div class="card-body">
                        <h5 class="card-title fw-bold">
                            @featured.Title
                        </h5>
                    </div>
                }
                else
                {
                    <div class="card-body">
                        <p>No featured news available</p>
                    </div>
                }
            </div>
        </div>

        <!-- Archive Section -->
        <div class="col-md-5">
            <div class="d-flex justify-content-between align-items-center mb-3">
                <h4 class="fw-bold">Archive</h4>
                <a class="text-danger fw-semibold" asp-action="Index" asp-controller="News">View All</a>
            </div>
            @if (Model != null && Model.Any())
            {
            @foreach (var newsItem in Model.OrderByDescending(n => n.CreatedAt).Take(4))
                    {

                <div class="d-flex mb-4">
                    <img src="/@newsItem.ImageId" class="me-3 rounded" style="width: 80px; height: 60px; object-fit: cover;" alt="News 1">
                <div>
                        <span class="text-uppercase text-muted small">@newsItem.Category</span>
                        <h6 class="mb-1 fw-semibold">@newsItem.Title</h6>
                        <small class="text-muted">@newsItem.CreatedAt.ToString("MMMM dd, yyyy")</small>
                </div>
            </div>
                   }
            }
            else
            {
                <p>No news  available.</p>
            }
            @* <div class="d-flex mb-4">
                <img src="/images/banner-image-DQggPpNB.jpg" class="me-3 rounded" style="width: 80px; height: 60px; object-fit: cover;" alt="News 2">
                <div>
                    <span class="text-uppercase text-muted small">Product</span>
                    <h6 class="mb-1 fw-semibold"> A New Era of Intelligent Assistance</h6>
                    <small class="text-muted">May 22, 2025</small>
                </div>
            </div>

            <div class="d-flex mb-4">
                <img src="~/images/banner-image-DQggPpNB.jpg" class="me-3 rounded" style="width: 80px; height: 60px; object-fit: cover;" alt="News 3">
                <div>
                    <span class="text-uppercase text-muted small">Press Release</span>
                    <h6 class="mb-1 fw-semibold">jonneli for Possibility, Progress, and Prosperity</h6>
                    <small class="text-muted">May 15, 2025</small>
                </div>
            </div>

            <div class="d-flex mb-4">
                <img src="~/images/banner-image-DQggPpNB.jpg" class="me-3 rounded" style="width: 80px; height: 60px; object-fit: cover;" alt="News 3">
                <div>
                    <span class="text-uppercase text-muted small">Press Release</span>
                    <h6 class="mb-1 fw-semibold">jonneli for Possibility, Progress, and Prosperity</h6>
                    <small class="text-muted">May 15, 2025</small>
                </div>
            </div> *@

        </div>
    </div>
</div>